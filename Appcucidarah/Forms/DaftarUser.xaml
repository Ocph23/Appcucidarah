<Page x:Class="Appcucidarah.Forms.DaftarUser"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="DaftarUser">

    <Grid Style="{StaticResource ContentRoot}">
        <DataGrid Style="{StaticResource myDataGrid}"  ItemsSource="{Binding Collection.SourceView}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nama" Binding="{Binding NamaLengkap}"/>
                <DataGridTextColumn Header="User Name" Binding="{Binding UserName}"/>
                <DataGridTextColumn Header="Nomor Telepon" Binding="{Binding Kontak.NomorTelepon}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
