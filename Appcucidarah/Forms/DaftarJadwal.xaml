<Page x:Class="Appcucidarah.Forms.DaftarJadwal"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="DaftarJadwal">

    <Grid Style="{StaticResource ContentRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="55"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.RowSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="No"/>
                <DataGridTextColumn Header="Hari Pertama"/>
                <DataGridTextColumn Header="Hari Kedua"/>
                <DataGridTextColumn Header="Shif"/>
                <DataGridTextColumn Header="Jam Mulai"/>
                <DataGridTextColumn Header="Jam Selesai"/>
            </DataGrid.Columns>
        </DataGrid>
        <WrapPanel Grid.ColumnSpan="2" Grid.Row="1">
            <Button Command="{Binding TambahCommand}" Content="Tambah" Style="{StaticResource myButtonSuccess}"/>
            <Button  Command="{Binding EditCommand}" Content="Edit" Style="{StaticResource myButtonInfo}"/>
            <Button  Command="{Binding HapusCommand}" Content="Hapus" Style="{StaticResource myButtonError}"/>
        </WrapPanel>

    </Grid>
</Page>
